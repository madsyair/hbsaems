% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan-helpers.R
\name{build_brms_custom_family}
\alias{build_brms_custom_family}
\title{Build a brms Custom Family + Stanvars Pair from a Single Spec}
\usage{
build_brms_custom_family(
  name,
  dpars,
  links,
  lb = NA,
  ub = NA,
  type = c("real", "int"),
  loop = FALSE,
  log_lik = NULL,
  posterior_predict = NULL,
  posterior_epred = NULL
)
}
\arguments{
\item{name}{Character.  Family name; must match a
\code{<name>.stan} file under \code{inst/stan/}.}

\item{dpars}{Character vector of distributional parameter names.  The
first MUST be \code{"mu"} (brms convention).}

\item{links}{Character vector of link functions, same length as
\code{dpars}.  Common values: \code{"identity"}, \code{"log"},
\code{"logit"}.}

\item{lb, ub}{Numeric vectors of lower / upper bounds; \code{NA} for none.}

\item{type}{\code{"real"} (continuous) or \code{"int"} (discrete).}

\item{loop}{Logical.  \code{FALSE} (default) selects the vectorised brms
API (Stan signatures take vectors of \code{y} and \code{mu}); \code{TRUE}
selects scalar Stan signatures.  The default matches the neodistr
convention.  Whichever you choose, the corresponding \code{.stan}
file under \code{inst/stan/} must use the same convention.}

\item{log_lik}{Optional function for computing observation-level
log-likelihoods (used by \code{loo()}, \code{waic()}).  Signature
must be \code{function(i, prep)}.  See the brms vignette
"Define Custom Response Distributions" for details.}

\item{posterior_predict}{Optional function for drawing from the posterior
predictive distribution (used by \code{pp_check()},
\code{posterior_predict()}).  Signature
\code{function(i, prep, ...)}.}

\item{posterior_epred}{Optional function returning the conditional
expectation \eqn{E[Y \mid X]} (used by \code{fitted()},
\code{conditional_effects()}).  Signature
\code{function(prep)}.}
}
\value{
A list with two elements:
\describe{
  \item{\code{custom_family}}{A \code{brms::customfamily} object.}
  \item{\code{stanvars_family}}{A \code{brms::stanvars} object with the
    Stan code in the \code{functions} block.}
}
}
\description{
Convenience function that combines \code{\link[brms]{custom_family}} and
\code{\link[brms]{stanvar}} into the standard \code{list(custom_family,
stanvars_family)} pair returned by every \code{brms_custom_*} helper in
\pkg{hbsaems}.  The Stan code is read directly from
\code{inst/stan/<name>.stan} -- the user does not need to maintain it as
an R string.
}
\details{
Stan code is built once per call; if your code is reused many times,
wrap the returned object in a function and call it lazily.
}
\examples{
\donttest{
library(hbsaems)
library(brms)

# Build the loglogistic family from inst/stan/loglogistic.stan
ll <- build_brms_custom_family(
  name  = "loglogistic",
  dpars = c("mu", "beta"),
  links = c("log", "log"),
  lb    = c(0,    0),
  ub    = c(NA,   NA),
  type  = "real"
)
class(ll$custom_family)
}

}
\seealso{
\code{\link{read_stan_function}},
  \code{\link{register_hbsae_brms_custom}}.
}
