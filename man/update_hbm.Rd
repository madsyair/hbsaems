% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update-hbm.R
\name{update_hbm}
\alias{update_hbm}
\title{Update a Fitted HBM}
\usage{
update_hbm(
  object,
  newdata = NULL,
  formula. = NULL,
  iter = NULL,
  warmup = NULL,
  chains = NULL,
  cores = NULL,
  control = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{hbmfit} object.}

\item{newdata}{Optional replacement \code{data.frame}.}

\item{formula.}{Optional new formula (note the trailing dot, following
\code{stats::update}).  Pass \code{. ~ . + new_predictor} to add a term.}

\item{iter}{Optional new total number of iterations.}

\item{warmup}{Optional new warm-up length.}

\item{chains}{Optional new number of MCMC chains.}

\item{cores}{Optional new number of cores.}

\item{control}{Optional new control list (e.g.\ \code{list(adapt_delta = 0.99)}).}

\item{...}{Additional arguments forwarded to \code{\link[brms]{update.brmsfit}}.}
}
\value{
An updated \code{hbmfit} object.
}
\description{
Refits an \code{hbmfit} model with one or more arguments changed.  Useful
for re-running with longer chains, more iterations, or new data without
retyping the full \code{\link{hbm}} call.
}
\examples{
\donttest{
library(hbsaems)
library(brms)
data("data_fhnorm")
model <- hbm(brms::bf(y ~ x1), data = data_fhnorm,
             re = ~ (1 | group),    # area-level random effect
             chains = 2, iter = 1000, warmup = 500, cores = 1,
             seed = 1, refresh = 0)

# Re-run with more iterations
model2 <- update_hbm(model, iter = 4000, warmup = 2000)

# Add a predictor
model3 <- update_hbm(model, formula. = . ~ . + x2)
}
}
