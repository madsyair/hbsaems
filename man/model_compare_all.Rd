% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-compare.R
\name{model_compare_all}
\alias{model_compare_all}
\title{Compare Multiple Fitted HBMs}
\usage{
model_compare_all(..., criterion = c("loo", "waic", "both"))
}
\arguments{
\item{...}{Named \code{hbmfit} objects.}

\item{criterion}{One of \code{"loo"} (default), \code{"waic"}, or
\code{"both"}.}
}
\value{
A \code{hbm_table} (a sorted \code{data.frame}) with columns
  \code{Model}, \code{ELPD_LOO}, \code{LOO_SE}, \code{LOO_rank} (and
  analogous \code{*_WAIC*} columns when requested).
}
\description{
Ranks N models by LOO and/or WAIC, returning a sorted \code{hbm_table}.
Analogous to \code{\link[brms]{loo_compare}}.
}
\examples{
\donttest{
library(hbsaems)
library(brms)
data("data_fhnorm")
FAST <- list(chains = 4, iter = 2000, warmup = 1000, cores = 1,
             seed = 1, refresh = 0)
m1 <- do.call(hbm, c(list(formula = brms::bf(y ~ x1),
                          data = data_fhnorm), FAST))
m2 <- do.call(hbm, c(list(formula = brms::bf(y ~ x1 + x2),
                          data = data_fhnorm), FAST))
model_compare_all(simple = m1, medium = m2)
}
}
