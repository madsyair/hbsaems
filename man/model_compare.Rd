% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-compare.R
\name{model_compare}
\alias{model_compare}
\title{Compare One or Two Fitted HBMs}
\usage{
model_compare(
  model,
  model2 = NULL,
  ndraws_ppc = 100,
  moment_match = FALSE,
  moment_match_args = list(),
  reloo_args = list(),
  plot_types = c("pp_check", "params"),
  comparison_metrics = c("loo", "waic", "bf"),
  run_prior_sensitivity = FALSE,
  sensitivity_vars = NULL,
  ...
)
}
\arguments{
\item{model}{An \code{hbmfit} or \code{brmsfit} object.}

\item{model2}{Optional second \code{hbmfit} for pairwise comparison.}

\item{ndraws_ppc}{Number of draws for the posterior-predictive plot
(default \code{100}).}

\item{moment_match}{Logical; use moment matching for LOO
(default \code{FALSE}).}

\item{moment_match_args}{Named list of arguments for moment matching.}

\item{reloo_args}{Named list of arguments for \code{\link[brms]{reloo}}.}

\item{plot_types}{Character vector.  Any subset of
\code{c("pp_check", "params")}.}

\item{comparison_metrics}{Character vector.  Any subset of
\code{c("loo", "waic", "bf")}.}

\item{run_prior_sensitivity}{Logical; run prior sensitivity analysis using
\pkg{priorsense} (default \code{FALSE}).}

\item{sensitivity_vars}{Variables for the sensitivity analysis.}

\item{...}{Additional arguments.}
}
\value{
An \code{hbmc_results} object with components \code{loo1},
  \code{waic1}, \code{pp_check}, \code{params}, and -- when \code{model2}
  is given -- also \code{loo2}, \code{waic2}, \code{bf}, and
  \code{model2}.
}
\description{
Computes LOO, WAIC, and posterior predictive check diagnostics.  With a
single model this gives goodness-of-fit metrics; with two models it adds
a pairwise comparison.
}
\examples{
\donttest{
library(hbsaems)
library(brms)
data("data_fhnorm")
FAST <- list(chains = 2, iter = 2000, warmup = 1000, cores = 1,
             seed = 123, refresh = 0)

m1 <- do.call(hbm, c(list(formula = brms::bf(y ~ x1 + x2 + x3),
                          data = data_fhnorm), FAST))
m2 <- do.call(hbm, c(list(formula = brms::bf(y ~ x1 + x2),
                          data = data_fhnorm), FAST))

model_compare(m1)            # single-model goodness-of-fit
model_compare(m1, m2)        # pairwise comparison
}

}
\seealso{
\code{\link{model_compare_all}}, \code{\link{model_average}}
}
