% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior-methods.R
\name{posterior_draws}
\alias{posterior_draws}
\title{Extract Posterior Draws as a Matrix}
\usage{
posterior_draws(model, params = NULL, ...)
}
\arguments{
\item{model}{An \code{hbmfit} object.}

\item{params}{Optional character vector of parameter names; \code{NULL}
(default) returns all parameters.}

\item{...}{Additional arguments forwarded to
\code{\link[brms]{as_draws_matrix}}.}
}
\value{
A draws matrix (rows = MCMC iterations, columns = parameters).
}
\description{
Extract Posterior Draws as a Matrix
}
\examples{
\donttest{
library(hbsaems)
library(brms)
data("data_fhnorm")
model <- hbm(brms::bf(y ~ x1), data = data_fhnorm,
             re = ~ (1 | regency),    # area-level random effect
             chains = 2, iter = 1000, warmup = 500,
             cores = 1, seed = 1, refresh = 0)
draws <- posterior_draws(model)
dim(draws)
}
}
