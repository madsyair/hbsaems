% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist-loglogistic.R
\name{loglogistic}
\alias{loglogistic}
\alias{dloglogistic}
\alias{ploglogistic}
\alias{qloglogistic}
\alias{rloglogistic}
\title{Loglogistic Distribution Functions}
\usage{
dloglogistic(x, mu = 1, beta = 1, log = FALSE)

ploglogistic(q, mu = 1, beta = 1, lower.tail = TRUE, log.p = FALSE)

qloglogistic(p, mu = 1, beta = 1, lower.tail = TRUE, log.p = FALSE)

rloglogistic(n, mu = 1, beta = 1)
}
\arguments{
\item{x, q}{Vector of quantiles (\code{x > 0} for non-zero density).}

\item{mu}{Scale parameter (\code{mu > 0}; equals the median).}

\item{beta}{Shape parameter (\code{beta > 0}).}

\item{log, log.p}{Logical; if \code{TRUE}, return the log density / log
probability.}

\item{lower.tail}{Logical; if \code{TRUE} (default) probabilities are
\eqn{P[Y \le q]}, otherwise \eqn{P[Y > q]}.}

\item{p}{Vector of probabilities (\code{0 <= p <= 1}).}

\item{n}{Number of random draws.}
}
\value{
Numeric vector of the same length as the input.
}
\description{
Density, distribution function, quantile function, and random generation
for the loglogistic distribution with scale parameter \code{mu > 0} and
shape parameter \code{beta > 0}.
}
\details{
The probability density function is
\deqn{f(y) = (\beta/\mu) (y/\mu)^{\beta - 1}
         \{1 + (y/\mu)^{\beta}\}^{-2}, \quad y > 0,}
with cumulative distribution function
\deqn{F(y) = \{1 + (y/\mu)^{-\beta}\}^{-1}.}
}
\examples{
dloglogistic(c(0.5, 1, 2),  mu = 1, beta = 2)
ploglogistic(c(0.5, 1, 2),  mu = 1, beta = 2)
qloglogistic(c(0.25, 0.75), mu = 1, beta = 2)
set.seed(1); rloglogistic(5, mu = 1, beta = 2)

}
