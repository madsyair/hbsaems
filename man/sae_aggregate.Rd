% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sae-predict.R
\name{sae_aggregate}
\alias{sae_aggregate}
\title{Aggregate Predictions from Multiple hbsae_results}
\usage{
sae_aggregate(..., method = c("mean", "median", "weighted"), weights = NULL)
}
\arguments{
\item{...}{Two or more \code{hbsae_results} objects.}

\item{method}{One of \code{"mean"} (default), \code{"median"}, or
\code{"weighted"}.}

\item{weights}{Numeric vector of weights, required when
\code{method = "weighted"}.  Internally normalised to sum to 1.}
}
\value{
An \code{hbsae_results} object containing the combined predictions.
}
\description{
Combines area-level predictions across multiple \code{hbsae_results}
objects.  All objects must report predictions for the same number of areas
(in the same order).
}
\examples{
p1  <- structure(list(result_table = data.frame(Prediction = 1:3,
                                                 RSE_percent = c(5, 5, 5)),
                       rse_model = 5, pred = 1:3),
                  class = "hbsae_results")
p2  <- structure(list(result_table = data.frame(Prediction = 2:4,
                                                 RSE_percent = c(4, 4, 4)),
                       rse_model = 4, pred = 2:4),
                  class = "hbsae_results")
sae_aggregate(p1, p2, method = "mean")
sae_aggregate(p1, p2, method = "weighted", weights = c(0.6, 0.4))

}
