% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hbm-config.R
\name{hbm_control}
\alias{hbm_control}
\title{Sampler Configuration for HBSAE Models}
\usage{
hbm_control(
  chains = 4L,
  iter = 4000L,
  warmup = NULL,
  thin = 1L,
  cores = 1L,
  seed = NULL,
  refresh = NULL,
  adapt_delta = NULL,
  max_treedepth = NULL,
  control = NULL
)
}
\arguments{
\item{chains}{Integer.  Number of Markov chains (default \code{4L}).}

\item{iter}{Integer.  Total iterations per chain (default \code{4000L}).}

\item{warmup}{Integer.  Warm-up iterations per chain.  Default
\code{floor(iter / 2)}.}

\item{thin}{Integer.  Thinning interval (default \code{1L}).}

\item{cores}{Integer.  Number of cores for parallel chains
(default \code{1L}).}

\item{seed}{Optional integer seed for reproducibility.}

\item{refresh}{Integer.  Stan progress refresh frequency
(default \code{NULL}: \pkg{brms} default).}

\item{adapt_delta}{Numeric in \eqn{(0, 1)}.  When supplied, included
in the \code{control} list as \code{control = list(adapt_delta = ...)}.}

\item{max_treedepth}{Integer.  Max tree depth, included in
\code{control} when supplied.}

\item{control}{Optional \code{list} of additional NUTS control
options.  Merged with any \code{adapt_delta} / \code{max_treedepth}
above.}
}
\value{
A named list whose elements are valid arguments of
  \code{\link{hbm}}.
}
\description{
Bundles the MCMC sampler arguments of \code{\link{hbm}} (and the
\code{hbm_*} family wrappers) into a single named list.  Use this when
you want a reusable sampler profile or to reduce the size of long
\code{hbm()} calls.
}
\details{
This is entirely \strong{opt-in}: the flat signatures of \code{hbm()},
\code{hbm_lnln()}, etc.\ continue to work exactly as before.  Pass the
result directly to \code{hbm()} -- it is auto-spliced via
\code{...}.
}
\examples{
# Build a reusable "high-quality" profile
hq <- hbm_control(chains = 4, iter = 8000, cores = 4,
                  adapt_delta = 0.99, seed = 1)
str(hq)

# Quick draft profile
draft <- hbm_control(chains = 2, iter = 1000)

}
\seealso{
\code{\link{hbm_priors}}, \code{\link{hbm_nonlinear}},
  \code{\link{hbm}}
}
