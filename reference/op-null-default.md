# Default Value for NULL

Equivalent to the rlang `%||%` operator. Internal use only.

## Usage

``` r
x %||% y
```

## Arguments

- x, y:

  Any R objects.

## Value

`x` if non-`NULL`, otherwise `y`.
